run_sum0 (4964141):
  note: Easier constraint when trying to vote:
        instead of having to sample each location at least once, vote is accepted if an agent experienced at
        least one event (anywhere)
  experiment:
  num_agents: 50
  num_locations: 2
  lambda_min: 0.1
  lambda_max: 1
  blue_env_lambda: 0.00005
  red_env_lambda_easy: 0.0001
  red_env_lambda_hard: 6.666666666666667e-5
  current_hardness: "easy"
  travel_time: 10
  quorum_threshold: 0.7
  gamma_epsilon: 0.01
  # (!) timesteps, not agent steps
  max_steps: 1e6
  min_sampling_duration: 2000
  test_run_name: "run_sum0_checkpoint-0"
  training: true
  load_checkpoint: false
  training_iterations: 800
  eval_iterations: 1
  base_seed: 462

rewards:
  reward_per_event: 0.0
  reward_for_correct_decision: 10
  reward_for_wrong_decision: -50
  decision_cost: -0.005
  # 1e7, 1e7 * 5e-6 = 50
  # solving bonus: solved_reward - steps * solved_bonus_time_decay 
  # The solving bonus should be >=0 when reaching max steps, otherwise it penalizes correct solving
  solved_bonus_time_decay: 5e-7
  progress_bonus: 1

run_Easy_democrat (4964162):
  note: Can only vote for locations where it has experienced at least 1 event.
  experiment:
  num_agents: 50
  num_locations: 2
  lambda_min: 0.1
  lambda_max: 1
  blue_env_lambda: 0.00005
  red_env_lambda_easy: 0.0001
  red_env_lambda_hard: 6.666666666666667e-5
  current_hardness: "easy"
  travel_time: 10
  quorum_threshold: 0.7
  gamma_epsilon: 0.01
  # (!) timesteps, not agent steps
  max_steps: 1e6
  min_sampling_duration: 2000
  test_run_name: "run_Easy_democrat_checkpoint-0"
  training: true
  load_checkpoint: false
  training_iterations: 800
  eval_iterations: 1
  base_seed: 462

rewards:
  reward_per_event: 0.0
  reward_for_correct_decision: 10
  reward_for_wrong_decision: -50
  decision_cost: -0.005
  # 1e7, 1e7 * 5e-6 = 50
  # solving bonus: solved_reward - steps * solved_bonus_time_decay 
  # The solving bonus should be >=0 when reaching max steps, otherwise it penalizes correct solving
  solved_bonus_time_decay: 5e-7
  progress_bonus: 1